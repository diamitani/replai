# Supabase — from dashboard / CLI link
NEXT_PUBLIC_SUPABASE_URL=https://potuzocstvlrlmlrneid.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_publishable_or_anon_key_here
SUPABASE_PROJECT_REF=potuzocstvlrlmlrneid

# Server-only (optional for admin scripts; get from Supabase dashboard → API)
SUPABASE_SERVICE_ROLE_KEY=

# Local CLI / migrations only — never expose to client
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.potuzocstvlrlmlrneid.supabase.co:5432/postgres

# DeepSeek — primary LLM (direct API, not OpenRouter)
DEEPSEEK_API_KEY=your_deepseek_api_key
LLM_MODEL=deepseek-chat
DEEPSEEK_LOW_BALANCE_USD=5

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
