# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://potuzocstvlrlmlrneid.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_publishable_or_anon_key_here
SUPABASE_PROJECT_REF=potuzocstvlrlmlrneid

# Server-only (optional admin scripts — Supabase → Settings → API)
SUPABASE_SERVICE_ROLE_KEY=

# Local CLI / migrations only — never expose to client
DATABASE_URL=postgresql://postgres.YOUR_PROJECT_REF:YOUR_PASSWORD@aws-1-us-east-2.pooler.supabase.com:6543/postgres?sslmode=require

# DeepSeek
DEEPSEEK_API_KEY=your_deepseek_api_key
LLM_MODEL=deepseek-chat
DEEPSEEK_LOW_BALANCE_USD=5

# App URLs
# Local:
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Production (set this on Vercel):
# NEXT_PUBLIC_APP_URL=https://replai-psi.vercel.app

# Auth callbacks (configure in Supabase URL Configuration — not env vars)
# Local:      http://localhost:3000/auth/callback
# Production: https://replai-psi.vercel.app/auth/callback
# Google OAuth redirect (Google Cloud Console):
#   https://potuzocstvlrlmlrneid.supabase.co/auth/v1/callback
#
# Full checklist: AUTH.md
